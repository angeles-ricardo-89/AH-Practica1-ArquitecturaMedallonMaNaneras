APP_ENV=local
SOURCE_ARCHIVE_URL=https://www.gob.mx/presidencia/es/archivo/articulos

POSTGRES_HOST=localhost
POSTGRES_DB=mananeras
POSTGRES_USER=mananeras
POSTGRES_PASSWORD=mananeras

DUCKLAKE_CATALOG=postgres
DUCKLAKE_DATA_PATH=/data/lakehouse/ducklake_files.duckdb

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBED_MODEL=embeddinggemma

LLAMACPP_BASE_URL=http://localhost:9200/v1
LLAMACPP_MODEL=gemma-4-12b

RAG_TOP_K=8
MAX_CONTEXT_TOKENS=10000

# Auth (demo). En produccion los secretos deben ser >= 32 caracteres y salir de Secret Manager.
# Las credenciales demo se entregan solo en el PDF final; no se commitean valores reales.
JWT_SECRET=
CSRF_SECRET=
DEMO_USER_1_USERNAME=
DEMO_USER_1_PASSWORD=
DEMO_USER_2_USERNAME=
DEMO_USER_2_PASSWORD=

# Produccion (Gemini + Neon). Solo relevante con APP_ENV=production.
# GEMINI_API_KEY y NEON_DATABASE_URL provienen de Secret Manager; nunca se commitean valores reales.
GEMINI_API_KEY=
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
GEMINI_CHAT_MODEL=gemini-3.5-flash-lite
GEMINI_EMBEDDING_DIMENSION=768
INDEX_FORMAT_VERSION=v1
NEON_DATABASE_URL=

# Telegram (notificaciones best-effort). Token y chat id desde Secret Manager en prod.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
